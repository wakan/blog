box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - wakan/blog@0.2.1:
        token: $GIT_TOKEN
        domain: wakan.github.io
        basedir: public
        