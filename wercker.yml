box: debian
build:
  steps:
    - arjen/hugo-build: 
        flags: --buildDrafts=false
deploy:
  steps:
     uetchy/gh-pages: 
      token: $GITHUB_TOKEN 
      repo: wakan/wakan.github.io
      path: public
