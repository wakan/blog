box: debian
build:
  steps:
    - arjen/hugo-build: 
        flags: --buildDrafts=false
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        repo: wakan/blog
        basedir: public
