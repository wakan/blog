box: debian
build:
  steps:
    - arjen/hugo-build: 
        flags: --buildDrafts=false
deploy:
  steps:
     uetchy/gh-pages: 
      token: $GIT_TOKEN 
      repo: wakan/wakan.github.io
      path: public
