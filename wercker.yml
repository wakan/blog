box: debian
build:
  steps:
    - arjen/hugo-build: 
        theme: my-theme 
        flags: --buildDrafts=false
deploy:
  steps:
     uetchy/gh-pages: 
      token: $GITHUB_TOKEN 
      repo: wakan/blog
      path: public
